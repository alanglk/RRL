@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# RRL relies on FLogging, which is bundled in the same package installation
find_dependency(FLogging 1.0.0 EXACT)

# Include the auto-generated targets file
include("${CMAKE_CURRENT_LIST_DIR}/RRLTargets.cmake")

check_required_components(RRL)